<!--home.wxml  主页-->
<!--显示出会议简介-->
  <view wx:for="{{list}}" wx:if="{{item.state==2}}" id = "{{item.mid}}"bindtap="jumpToDetail" >
    <text>会议名称:{{item.name}}</text>
    <text>会议时间:{{item.time}}</text>
    <text>会议地点:{{item.place}}</text>
  </view>